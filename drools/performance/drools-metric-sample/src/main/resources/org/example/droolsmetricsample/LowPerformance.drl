package org.example.droolsmetricsample;

unit LowPerformanceRuleUnit;
import java.util.ArrayList;

rule "Collect expensive orders combination"
  when
    $p : /persons[ age < 20 ]
    $o1 : /orders[ person == $p ]
    $o2 : /orders[ person == $p, id > $o1.id, price > $o1.price ]
  then
end
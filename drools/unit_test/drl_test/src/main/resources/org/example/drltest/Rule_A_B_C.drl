package org.example.drltest;

rule "Rule_Aを記録"
    when
        $data : SampleData()
    then
        $data.addRuleName("Rule_A");
end

rule "Rule_Bを記録"
    when
        $data : SampleData()
    then
        $data.addRuleName("Rule_B");
end


rule "Rule_Cを記録"
    when
        $data : SampleData()
    then
        $data.addRuleName("Rule_C");
end